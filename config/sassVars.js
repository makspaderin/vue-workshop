// Sass variables to inject into build:
// see https://github.com/epegzz/sass-vars-loader

// The properties in the following module.exports can be used to
// overwrite the sass-variables defined in
// cloubi2-default-product-theme-components-vue/src/_variables.scss. These
// redefined sass-variables can also be used in this theme project. They
// are globally available without an explicit import statement.

// To apply universal changes in the theme (e.g. colours/animations/fonts),
// overwrite these variables.
module.exports = {
  'cloubi-color-attention': '#ff570e',
  'cloubi-navbar-height': '5.0rem',
  'cloubi-color-primary': '#ff570e',
  'cloubi-color-default': '#606470'
};
